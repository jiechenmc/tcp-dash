services:
  nginx:
    image: nginx
    ports:
      - 80:80
    volumes:
      - ./video-server/videos:/usr/share/nginx/html/media
      - ./video-server/user.conf:/etc/nginx/user_conf.d/user.conf
      - ./video-server/nginx.conf:/etc/nginx/nginx.conf
      - nginx_secrets:/etc/letsencrypt

volumes:
  nginx_secrets: